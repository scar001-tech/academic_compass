{
  "buildCommand": "pnpm --filter @workspace/academic-compass run build",
  "outputDirectory": "artifacts/academic-compass/dist/public",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
